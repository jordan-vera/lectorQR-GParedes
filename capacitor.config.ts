import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'exportadora-banano-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
